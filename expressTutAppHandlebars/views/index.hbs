<h1>{{ title }}</h1>
<p>Welcome to {{ title }}</p>

{{# if condition }}
    condition met
{{else}}
    condition not met
{{/if}}

{{# each array as |val key|}}
    <p> {{ key }} : {{ val }}</p>
{{/each}}